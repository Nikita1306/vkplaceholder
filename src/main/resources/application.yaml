spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/vktest
    username: postgres
    password: admin
    database: postgresql

    database-platform: org.hibernate.dialect.PostgreSQLDialect
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create
remote:
  home:
    https://jsonplaceholder.typicode.com
server:
  servlet:
    context-path: /api